spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:187.127.43.161}:${DB_PORT:3306}/${DB_NAME:db_dinix}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=${APP_TIMEZONE:America/Sao_Paulo}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:C0nV3Mu2026}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        type:
          preferred_uuid_jdbc_type: CHAR

logging:
  level:
    br.net.convertix.dinix: DEBUG
    org.hibernate.SQL: DEBUG
